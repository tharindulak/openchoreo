[project]
name = "portal-assistant"
version = "0.1.0"
description = "OpenChoreo control-plane copilot — read-only chat over the openchoreo MCP"
requires-python = ">=3.14"
dependencies = [
    "common",
    "authlib>=1.6.8",
    "fastapi>=0.141.1",
    "httpx>=0.23.0",
    "jinja2>=3.1.0",
    "langchain>=1.3.14",
    "langchain-mcp-adapters>=0.3.1",
    "langchain-openai>=1.4.1",
    "pyjwt[crypto]>=2.13.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.0",
    "uvicorn>=0.52.1",
    "mcp>=1.28.1,<2",
]

[tool.uv.sources]
common = { path = "../common", editable = true }

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.uv]
exclude-dependencies = ["zstandard", "pywin32"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py314"
extend-exclude = [".venv", "__pycache__", ".git"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "ARG", # flake8-unused-arguments
    "SIM", # flake8-simplify
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "B008",  # do not perform function calls in argument defaults
    "B904",  # raise from
]

[tool.ruff.lint.isort]
known-first-party = ["common", "src"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
