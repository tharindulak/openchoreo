apiVersion: openchoreo.dev/v1alpha1
kind: ProjectReleaseBinding
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: projectreleasebinding-sample
spec:
  # TODO(user): Add fields here
