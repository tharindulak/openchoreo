apiVersion: openchoreo.dev/v1alpha1
kind: ProjectRelease
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: projectrelease-sample
spec:
  # TODO(user): Add fields here
