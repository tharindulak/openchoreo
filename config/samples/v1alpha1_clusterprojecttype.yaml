apiVersion: openchoreo.dev/v1alpha1
kind: ClusterProjectType
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: clusterprojecttype-sample
spec:
  # TODO(user): Add fields here
