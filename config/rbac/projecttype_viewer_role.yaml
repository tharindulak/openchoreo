# permissions for end users to view projecttypes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: projecttype-viewer-role
rules:
- apiGroups:
  - openchoreo.dev
  resources:
  - projecttypes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - openchoreo.dev
  resources:
  - projecttypes/status
  verbs:
  - get
