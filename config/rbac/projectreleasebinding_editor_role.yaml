# permissions for end users to edit projectreleasebindings.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: projectreleasebinding-editor-role
rules:
- apiGroups:
  - openchoreo.dev
  resources:
  - projectreleasebindings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - openchoreo.dev
  resources:
  - projectreleasebindings/status
  verbs:
  - get
