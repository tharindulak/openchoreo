apiVersion: openchoreo.dev/v1alpha1
kind: Component
metadata:
  name: snip-analytics-service
  namespace: default
spec:
  owner:
    projectName: url-shortener

  componentType:
    kind: ClusterComponentType
    name: deployment/service
  autoDeploy: true

---
apiVersion: openchoreo.dev/v1alpha1
kind: Workload
metadata:
  name: snip-analytics-service
  namespace: default
spec:
  owner:
    componentName: snip-analytics-service
    projectName: url-shortener
  endpoints:
    http:
      type: HTTP
      port: 8081
  dependencies:
    endpoints:
      - project: url-shortener
        component: snip-redis
        name: tcp
        visibility: project
        envBindings:
          address: REDIS_ADDR
    resources:
      - ref: snip-postgres
        envBindings:
          url: POSTGRES_DSN
  container:
    image: ghcr.io/openchoreo/samples/snip-analytics-service:latest
    env:
      - key: PORT
        value: "8081"
      - key: OTEL_ENABLED
        value: "true"
      - key: OTEL_EXPORTER_ENDPOINT
        value: "http://opentelemetry-collector.openchoreo-observability-plane.svc.cluster.local:4318"
