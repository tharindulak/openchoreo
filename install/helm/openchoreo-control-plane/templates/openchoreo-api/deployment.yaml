{{- if .Values.openchoreoApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openchoreo-control-plane.openchoreoApi.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo-control-plane.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-server
  annotations:
    # Trigger rollout when config changes
    checksum/config: {{ include (print $.Template.BasePath "/openchoreo-api/configmap.yaml") . | sha256sum }}
spec:
  {{- if not .Values.openchoreoApi.autoscaling.enabled }}
  replicas: {{ .Values.openchoreoApi.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: api-server
      {{- include "openchoreo-control-plane.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: api-server
        {{- include "openchoreo-control-plane.selectorLabels" . | nindent 8 }}
        {{- include "openchoreo-control-plane.platformIdentityLabels" . | nindent 8 }}
      annotations:
        # Trigger pod restart when config changes
        checksum/config: {{ include (print $.Template.BasePath "/openchoreo-api/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "openchoreo-control-plane.openchoreoApi.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.openchoreoApi.priorityClass.create }}
      priorityClassName: {{ .Values.openchoreoApi.priorityClass.name }}
      {{- end }}
      {{- with .Values.openchoreoApi.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.openchoreoApi.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.openchoreoApi.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.openchoreoApi.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.openchoreoApi.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: api-server
        image: "{{ include "openchoreo-control-plane.image" (dict "context" . "image" .Values.openchoreoApi.image) }}"
        imagePullPolicy: {{ .Values.openchoreoApi.image.pullPolicy }}
        args:
          - --config=/etc/openchoreo/config.yaml
        ports:
        - containerPort: {{ .Values.openchoreoApi.config.server.port | default 8080 }}
          name: http
          protocol: TCP
        volumeMounts:
        - name: data
          mountPath: /var/lib/openchoreo/data
        - name: config
          mountPath: /etc/openchoreo
          readOnly: true
        - name: cluster-gateway-ca
          mountPath: /etc/cluster-gateway
          readOnly: true
        {{- if .Values.clusterGateway.internalMtls.enabled }}
        - name: cluster-gateway-client-tls
          mountPath: /etc/cluster-gateway-client
          readOnly: true
        {{- end }}
        {{- with .Values.openchoreoApi.config.remoteConnect }}
        {{- if .enabled }}
        - name: remote-connect-signing-key
          {{- /* Derived from signingKeyPath so the mount matches what the API reads. */}}
          mountPath: {{ dir (.signingKeyPath | default "/etc/remote-connect/signing.pem") }}
          readOnly: true
        {{- end }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- toYaml .Values.openchoreoApi.resources | nindent 12 }}
        {{- with .Values.openchoreoApi.containerSecurityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      volumes:
      - name: data
        emptyDir: {}
      - name: config
        configMap:
          name: {{ include "openchoreo-control-plane.openchoreoApi.name" . }}-config
      # Trust anchor for verifying the gateway. Only ca.crt is mounted; the
      # signing key in that secret stays out of this pod.
      - name: cluster-gateway-ca
        secret:
          secretName: {{ .Values.clusterGateway.tls.existingSecret | default .Values.openchoreoApi.clusterGateway.tls.caSecret }}
          items:
          - key: ca.crt
            path: ca.crt
      {{- if .Values.clusterGateway.internalMtls.enabled }}
      - name: cluster-gateway-client-tls
        secret:
          secretName: {{ .Values.openchoreoApi.clusterGateway.tls.clientSecret }}
      {{- end }}
      {{- with .Values.openchoreoApi.config.remoteConnect }}
      {{- if .enabled }}
      - name: remote-connect-signing-key
        secret:
          {{- /* Default matches remote-connect-signing-key.yaml. The Secret always stores
                 the key as signing.pem; project it under the configured filename. */}}
          secretName: {{ .signingKeySecretName | default "remote-connect-signing-key" }}
          items:
            - key: signing.pem
              path: {{ base (.signingKeyPath | default "/etc/remote-connect/signing.pem") }}
      {{- end }}
      {{- end }}
{{- end }}
