{{- if and .Values.clusterGateway.tlsRoute.enabled .Values.gateway.enabled }}
---
# TLSRoute for TLS passthrough
# Required for end-to-end mTLS with cluster agents
apiVersion: gateway.networking.k8s.io/v1
kind: TLSRoute
metadata:
  name: {{ include "openchoreo-control-plane.clusterGateway.name" . }}-tls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo-control-plane.labels" . | nindent 4 }}
    app.kubernetes.io/component: cluster-gateway
spec:
  parentRefs:
    - name: gateway-default
      namespace: {{ .Release.Namespace }}
      sectionName: tls-passthrough
  hostnames:
    {{- range .Values.clusterGateway.tlsRoute.hosts }}
    - {{ .host | quote }}
    {{- end }}
  rules:
    - backendRefs:
        - name: {{ include "openchoreo-control-plane.clusterGateway.name" . }}-external
          port: {{ .Values.clusterGateway.service.port }}
{{- end }}
